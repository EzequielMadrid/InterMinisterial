/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.clerk.com",
      "lh3.googleusercontent.com",
      "images.clerk.dev",
      "utfs.io",
    ],
  },
};

export default nextConfig;
